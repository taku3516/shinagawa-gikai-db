# 質問・答弁要約の作成ルール

本会議の代表質問・一般質問（`qaSummaries`）と、委員会質疑（`committeeSessions[].topics[].exchanges`）
の要約を作るときの共通ルール。会議録を遡って実装するとき、新しい会議録から要約を足すときは、
必ずこのルールに従い、最後に**検査**を通してから公開する。

## いちばん大事なこと

読む人が知りたいのは **「何について質問したか」「何について答弁したか」** の2つ。
要約がこれを伝えられていなければ、短くても長くても失敗している。

そのため次の2つを絶対に守る。

1. **文の途中で切らない。** 文字数の上限に当たったら、その手前の**文の区切りまで戻す**。
   語の途中で切って「…」を付けると、何について述べたのかが読み取れなくなる
2. **「答弁が無い」と「答弁を拾えなかった」を混ぜない。** 意見・要望のように答弁を
   求めていない発言と、答弁があるはずなのに拾えていない発言は、別の文言で書き分ける

## 判定と整形は1か所に集約する

ルールを生成・修復・検査の3か所に書くと必ずずれる。判定と整形はすべて
**`scripts/qa_summary.py`** に置き、次の3つがこれを読む。

| スクリプト | 役割 |
|---|---|
| `scripts/prepare_committees.py` | 委員会質疑を会議録から**生成**する |
| `scripts/build_heisei_year.py` | 本会議（平成年）を会議録から**生成**する |
| `scripts/build_from_minutes.py` | 質問者ページが無い年を会議録本文から**生成**する |
| `scripts/summarize_history.py` | 過去年の質問・答弁要約を**生成**する |
| `scripts/repair_qa_summaries.py` | すでに書き出したデータを**修復**する |
| `scripts/check_qa_summaries.py` | 公開前・変更時に**検査**する |

新しく要約を作るスクリプトを書くときも、切り詰め・語尾の変換・答弁なしの文言は
自分で書かずに `qa_summary` の関数を使う。

```python
import qa_summary as qa

question = qa.normalize_question(qa.finish(text, qa.QUESTION_LIMIT))
answer = qa.strip_answer_lead(qa.finish(text, qa.ANSWER_LIMIT)) or qa.no_answer_text(kind)
```

## スキーマ

本会議は質問エントリ（`questions[]`）に `qaSummaries` を持たせる。

```json
{ "title": "発言項目名（例: 防災について）",
  "question": "質問の要約（1〜2文・完結した文）",
  "answer": "答弁の要約（1〜2文・完結した文）" }
```

委員会は議題（`topics[]`）の下に `exchanges[]` を持たせる。`kind` は
質問／質問・要望／確認／提案／意見のいずれか。**答弁が無いときの文言は `kind` で決まる**ので、
`kind` を空にしない。

## 文体ルール

1. **必ず完結した文にする**。文末は「。」で終わる。途中で切れた抜粋を置かない
   - 上限に当たったら文の区切りまで戻す（`qa.finish`）
   - 語尾を付けてから文字数で切ると語尾自体が欠ける。**長さを整えてから語尾を付ける**
2. **質問は間接話法**で書く。議員の発言をそのまま接ぎ木しない
   - NG: 「議員は、〜についてお聞かせください。」（一人称の命令形が残る）
   - OK: 「議員は、〜を説明するよう求めました。」「〜の検討を求めました。」
   - **言い換えは動詞の形のまま行い、名詞句にしない。** 「教えてください」を
     「の説明を求めました」に置き換えると、直前が「を」のときに
     「その辺を説明を求めました」となって助詞が噛み合わなくなる。
     「説明するよう求めました」なら「を」「は」「が」のどれでも文が通る
   - 変換表: お聞かせ／お教え／ご説明ください→説明するよう求めました ／
     お答え／ご答弁ください→答えるよう求めました ／
     お示し／お知らせください→示すよう求めました ／ ご検討ください→検討を求めました ／
     確認させてください→確認したいと求めました ／ お伺いさせてください→尋ねました ／
     〜でしょうか／ですか→〜か尋ねました
   - 頼み方は数え切れないほどあるので、上のどれにも当てはまらない「〜ください」は
     **「〜いただくよう求めました」**で受ける（`QUESTION_ENDING_FIXES` の最後の規則）。
     規則を一つずつ足していくのではなく、受け皿を1つ置いて取りこぼしを無くす
3. **答弁は「区側は、」で始め、です・ます体**で書く
   - 「私からは」「お答えいたします」などの前置きだけの文は採用しない
   - **「区側は、まず初めに、」のような中身の無い前置きは落とす**（`qa.strip_answer_lead`）。
     限られた字数を中身の無い語で使わない
4. **長さの上限**: 質問190字・答弁240字（`qa.QUESTION_LIMIT` / `qa.ANSWER_LIMIT`）
5. **重複表現の禁止**: 「「〜について」について」のような二重表現を作らない
   - titleが「〜について」で終わるならその部分を除いてから定型文に当てはめる
6. **文頭・語頭の断片禁止**: 「ィーゼル車」のように語の先頭が欠けた抜粋を置かない
7. **短い答弁はそのままでよい**。「そのとおりです。」のような答弁は、画面では質問の
   すぐ下に並ぶため意味が通る。無理に長くしない

## 答弁が取れないときの文言

推測で答弁を作らない。次のどちらかを使う（`qa.no_answer_text(kind)` が選ぶ）。

| 発言の種類 | 文言 |
|---|---|
| 意見・提案 | 答弁を求める発言ではないため、これに対する答弁はありません。 |
| 質問・確認・要望 | この発言に対する答弁を会議録から特定できませんでした。会議録の全文をご確認ください。 |

質問の内容が取れないときの定型文は
「「{title}」について、現状の認識と区の対応を質問しました。」。
これは**取り直しが必要な印**であり、望ましい状態ではない。検査で件数が出る。

## 出典と免責（必須）

- 各質問エントリの `links` に、該当日の会議録（`Template=document&Id=N`）または
  会議録検索へのリンクを必ず含める
- 表示側の免責「※質問・答弁の要点を本サイトで要約しています。正確な発言は公式の
  会議録をご確認ください。」が出ることを確認する

## 生成手順（会議録からの機械抽出）

1. 会議録本文から発言スパンを切り出す（`〔○○君登壇〕`マーカー。役職者
   （区長・教育長・次長・部長・議長・委員長・監査・室長・所長・管理者）は答弁側）
2. 発言者ヘッダ（`N:◯区長（…）`）は必ず除去してから文抽出する
3. 質問がある日だけを対象にする（議事日程に「質問」の記載がある日。
   最終日の委員長報告・討論の登壇者を質問者に混ぜない）
4. テーマは質問文の「初めに／次に／第一に／まず初めは」等の列挙から抽出
5. 答弁の照合はテーマの先頭8字→6字→4字→末尾5字の順で試す。
   前置き文（60字未満で「お答え」を含む文）はスキップして次の実質文を採る
6. 要約の整形は `qa_summary` の関数に通す（自前で切らない）

## 検査（公開前に必ず実行）

```bash
python3 scripts/check_qa_summaries.py           # 件数だけ
python3 scripts/check_qa_summaries.py --details # 問題のある要約も表示
```

検査は3種類に分かれる。

- **要修正** — 空・文の途中で切れている・古い文言のまま・二重について・一人称・
  助詞の重複・語頭の欠落・字数超過。機械的に直せるので**0件でなければならない**。
  1件でもあると終了コード1になり、GitHub Actions（`.github/workflows/check-qa-summaries.yml`）が失敗する
- **要改善** — 「〜についてお伺いします」だけの質問、定型文のまま、短すぎる質問。
  会議録から取り直さないと直らない。件数を出すだけで失敗にはしない。
  再生成のたびに減らしていく
- **品質** — 文としては壊れていないが、発言をそのまま貼っただけで要約になって
  いないもの。**0件は求めず、割合を基準値と比べて悪化だけを止める**（下記）

## 品質の検査と基準値

### なぜ分けたか

要修正が0件でも、要約として読めていないことがある。実際にそうなっていた。
2026-08-09 の時点で要修正0件・要改善607件と表示されながら、実際には

- 委員会の質問の **40.9%** に話し言葉がそのまま残っていた
- 本会議 h15〜h29 の **46.8%** が見出しの言い換えだけで終わっていた
- 委員会の掲載量は原文の **約20%** で、要約ではなく抜粋になっていた

形だけを見る検査は「直したつもり」を通してしまう。だから割合を年ごとに記録して、
悪化したら落ちるようにした。

### 項目

`qa_summary.check_quality()` が返す。判定は1件ごと、割合の集計は検査側で行う。

| 項目 | 何を見るか |
|---|---|
| 質問:話し言葉 | 「その辺」「ちょっと」など、単独では指すものが分からない口語が残っている |
| 質問:タイトル反復 | 見出しの言い換えだけで終わり、何を尋ねたのかが入っていない |
| 質問:語尾破綻 | 語尾の差し替えが失敗している。大半は文末だと思って語尾を付けた結果、直後に接続助詞が続いて元の発言が後ろに残る型（「〜改修してほしいと求めましたが、いかがでしょうか。」）。残りは直前の助詞とぶつかった型（「構わないのでの説明を求めました」） |
| 答弁:答弁欠落 | 答弁を求める発言なのに答弁を特定できていない。**意見・提案は分母から外す**（答弁が無いのが正常な状態なので、混ぜると改善したか読めなくなる） |
| 答弁:話し言葉 | 同上 |

### 基準値（ラチェット）

`scripts/qa_baseline.json` に**年ごとの割合**を記録してある。現状が良いという
意味ではなく、**ここから悪くしない**ための線。悪化幅が許容幅（0.5ポイント。
会議の増減で割合がわずかに動く分を吸収する）を超えると終了コード1になる。

年ごとに持つのは、全体の平均だと局所的な破綻が薄まって見えなくなるため。
実際 r06〜r07 の定型文（19〜28%）は、344件 / 165,972件 の全体平均では消える。

生成を直して良くなったら引き直す。良くなった値で線を引き直すので一方向にしか動かない。

```bash
python3 scripts/check_qa_summaries.py --by-year          # 年ごとの内訳
python3 scripts/check_qa_summaries.py --update-baseline  # 改善後に引き直す
```

### 2026-08-09 時点の実測（基準値の初期値）

**委員会（161,834件・全26年）は年による断層がない。** 保持率18〜22%、語尾破綻
1〜3%、答弁欠落13.5〜21.9% がほぼ一様。唯一の勾配は話し言葉の残存率で、
h21 の 50.5% を最悪として r05 の 29.7% まで緩やかに改善している。ただし最良の
年でも3件に1件が話し言葉のままで、原因は年ごとの差ではなく
`prepare_committees.py` の設計そのものにある。

**本会議（4,138件）は作り方で4つに割れる。**

| 区間 | 件数 | タイトル反復 | 答弁欠落 | 作り方 |
|---|---:|---:|---:|---|
| h13〜h14 | 190 | 61.1% | 21.1% | 機械生成（語尾変換なし） |
| h15〜h29 | 2,537 | 46.8% | 25.1% | 機械生成 |
| **h30〜r05** | **1,029** | **7.5%** | **0.0%** | **バッチ要約** |
| r06〜r07 | 344 | 19.2% | 0.0% | 機械生成（バッチ未完了） |
| r08〜 | 38 | 0.0% | 0.0% | 進行中 |

読める水準にあるのは h30〜r05 の1,029件だけで、全165,972件の **0.6%** にあたる。

この帯だけが良いのはスクリプトの差ではなく**入力の差**。`build_history_supplements.py`
は「qaSummaries を入力してから実行する」設計で、その入力を作るのが
`scripts/prompts/history-qa-summary.md`（AI支援のバッチ要約）。h30〜r07 はすべて
`prepare_history.py` を通っているが、バッチ要約が入っているのは r05 まで。
見分けは答弁の書き出しで付く。

- **「区は、」** → バッチ要約の産物（h30〜r05 で92〜97%）
- **「区側は、」** → `qa_summary` を通した機械生成（h13〜h29、r06〜r07）

つまり **h30〜r05 は到達可能な水準の実物**であり、r06〜r07 は同じ工程が
途中で止まった状態。優先度は r06〜r07（344件）→ h15〜h29（2,537件）→
委員会（161,834件）の順で費用対効果が高い。

すでに書き出したデータに要修正が出た場合は、まず次を試す。

```bash
python3 scripts/repair_qa_summaries.py           # 何件直るかを確認
python3 scripts/repair_qa_summaries.py --write   # 実際に直す
```

修復できるのは「完結した文まで戻す」「前置きを落とす」「語尾を整える」までで、
切り落とされた文そのものは戻らない。それ以上は会議録からの再生成が必要になる。

## 修正の経緯

- 2026-07-22 全年1,357項目を検査し678項目+246項目を修正
- 2026-08-07 判定を `scripts/qa_summary.py` に集約。文字数で切って「…」を付けていた
  実装を、文の区切りまで戻す実装に置き換え、**54,404件**を修復した
  （委員会52,451件・本会議1,953件）。要修正は0件になり、要改善607件（全体の0.4%）が残る
- 2026-08-07 2017年（平成29年）の委員会質疑を会議録から作り直し、質問1,287件・
  答弁380件・議題名83件の文言が直った。要改善は**委員会79件・本会議529件**に分かれており、
  委員会側はほぼ取り切っている。残りを減らすには本会議側の作り直しが要る
- 2026-08-07 検査が「〜してください」「〜でしょうか」を見逃していたため、委員の発言を
  そのまま残した要約が全年に残っていた。検査を語尾全体に広げ、語尾の言い換えを
  動詞の形に改めたうえで**9,748件**を修復した（一人称3,875件・助詞の重複5,873件）。
  言い換えの規則は `qa_summary.QUESTION_ENDING_FIXES` に集約し、
  `prepare_committees.py` が持っていた同じ変換は消して共通ルールを呼ぶようにした
- 2026-08-09 検査が形だけを見ていて、要修正0件のまま品質が把握できていなかった。
  意味レベルの判定（`qa_summary.check_quality`）を足し、年ごとの割合を
  `scripts/qa_baseline.json` に基準値として記録して、悪化したら失敗する方式にした。
  データは変更していない（測り方だけを直した）。実測でわかったこと:
  委員会は全26年が一様に不十分で断層がなく、本会議は作り方で4つに割れ、
  読める水準にあるのは h30〜r05 の1,029件（全体の0.6%）だけだった
